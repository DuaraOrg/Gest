@page "/clients"
@using Gest.UI._Features_.Clients.Components
@layout AdminLayout

<PageTitle>Clients</PageTitle>
<TopAdminNav Title="Gestion de clients" />
<div class="h-full mt-14 lg:mt-16 p-0" style="padding:0rem;">
    <List/>
</div>
@code {

    [Inject] AdminNavState AdminNavState { get; set; } = null!;
  

    protected override Task OnAfterRenderAsync(bool firstRender)
    {
        if (firstRender)
        {
            AdminNavState.CurrentUrl = UrlKeys.Clients;
        }
        return base.OnAfterRenderAsync(firstRender);
    }
}